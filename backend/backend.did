type File = 
 record {
   content_type: text;
   data: blob;
   name: text;
 };
service : {
  getFile: (text) -> (opt File) query;
  listFiles: () -> (vec text) query;
  uploadFile: (text, text, vec nat8) -> (text);
}
