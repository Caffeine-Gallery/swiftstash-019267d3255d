type FileInfo = 
 record {
   content: vec nat8;
   contentType: text;
 };
service : {
  deleteFile: (text) -> ();
  getFileInfo: (text) -> (opt FileInfo) query;
  listFiles: () -> (vec text) query;
  uploadFile: (text, text, vec nat8) -> (text);
}
