type File = 
 record {
   content_type: text;
   data: blob;
   name: text;
 };
service : {
  getFile: (text) -> (opt File) query;
  listFiles: () -> (vec text) query;
  uploadFileChunk: (text, text, blob, nat, nat) -> (nat);
}
